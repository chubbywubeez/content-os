# Deploy from **repository root** (this folder), not `apps/copy-maker/` alone — the Dockerfile copies `data/` and app files.
# Build: Docker. Start command is defined by the Dockerfile (`apps/copy-maker/scripts/railway-start.mjs`).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
